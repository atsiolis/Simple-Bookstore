#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "project_lib.h"

int main(int argc, char *argv[])
{
    int choice;
    choice = menu();
    while (choice)
    {
     if (choice == 1){}   
     else if(choice == 2){}
     else if(choice == 3){}
     else if(choice == 4){}
     else if(choice == 5){}
     else if(choice == 6){}
     else if(choice == 7) return 0;
     choice = menu();
    }
    return 0;
}